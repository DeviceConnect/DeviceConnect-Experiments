<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_margin="8dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/device_type_image"
        android:layout_margin="8dp"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_centerVertical="true"
        android:layout_centerInParent="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:src="@drawable/unconfirmed"
        />

    <LinearLayout
        android:orientation="vertical"
        android:layout_centerInParent="true"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/btn_connect_device"
        android:layout_toRightOf="@+id/device_type_image"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/device_name"
            android:text="@string/heart_rate_setting_default_name"
            android:textSize="@dimen/text_size_setting_device_name"
            android:textStyle="bold"
            android:textColor="@color/item_name_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/device_address"
            android:text="@string/heart_rate_setting_default_address"
            android:textSize="@dimen/text_size_setting_device_address"
            android:textColor="@color/item_address_text"
            android:paddingLeft="8dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </LinearLayout>

    <Button
        android:id="@+id/btn_connect_device"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/button_blue"
        android:layout_margin="8dp"
        android:text="@string/heart_rate_setting_register"
        android:layout_centerInParent="true"
        android:layout_alignParentRight="true"
        android:textColor="#FFFFFF"/>

</RelativeLayout>